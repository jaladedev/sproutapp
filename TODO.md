# SproutVest Frontend — Todo List

> Generated from code review of `reuvest/frontend` (Next.js 16 + React 19 + Tailwind CSS v4)

---

## 🔴 Critical — Do First

| # | Task | Impact |
|---|------|--------|
| 1 | ~~Replace the boilerplate README~~ ✅ | Rewrote `README.md` with real stack info, env var table (sourced from an actual `grep` of `process.env` usage across the repo, not guessed), auth flow summary, and project structure. Also added a `.env.example` since none existed and the README references it. |
| 2 | **Add TypeScript** — incremental adoption, tooling in place, `app/settings/`, all auth pages, and all of `app/admin/` now fully converted | Added `typescript`, `@types/react-dom` to `package.json`, plus a `typecheck` script (`tsc --noEmit`). Replaced `jsconfig.json` with `tsconfig.json` (`allowJs: true`, `strict: true`) so `.js`/`.jsx` keep working unconverted while new/converted files use `.ts`/`.tsx`. `next-env.d.ts` regenerated and un-ignored (was masking null-safety bugs — see original fix notes below). `global.d.ts` added for `*.css`/`*.svg`/image ambient modules. `tsc --noEmit` passes clean repo-wide as of every commit in this log. <br><br>**`app/settings/`, all 6 auth pages, and all of `app/admin/` are 100% `.tsx`/`.ts`** — every admin page converted: `referrals`, `lands` list + `PolygonMapEditor`, `lands/create`, `lands/[id]/edit`, `waitlist`, `kyc`, `compliance`, `blog`, `withdrawals`, root dashboard, `live-chat`, `support`, `users`. Also swept `useConfirm()` (zustand, #15) across admin pages in place of `window.confirm()`, and standardized admin loading spinners. <br><br>**Real bugs caught in the admin batch:** `admin/live-chat/page.jsx` called `api.post(...)` in its broadcast authorizer but never imported `api` anywhere in the file — a `ReferenceError` waiting to fire the instant an agent opened a private chat channel; fixed. That same authorizer was passing `true`/`false` booleans where pusher-js's real `ChannelAuthorizationCallback` contract expects `(Error | null, authData | null)` — worked by accident (truthy/falsy), but silently discarded the actual error object on failure; fixed to match the real typed contract. `admin/waitlist` and `admin/blog` and `admin/support` all built `new URLSearchParams({ page: <number>, ... })` — works via silent coercion in plain JS, invalid under `URLSearchParams`'s real TS constructor signature (`Record<string, string>`); fixed by stringifying explicitly in each. Several admin list/detail pages (`kyc`, `compliance`, `waitlist`, `referrals`, `blog`, `lands`) call into the new service layer (#17) whose list/detail endpoints intentionally return `unknown` since they're shared across many consumers — each page asserts the service's return into its own specific local interface at the single fetch call site, documented inline, rather than widening the shared service types for every other consumer. <br><br>**`PolygonMapEditor.tsx` — a notable cross-session type-resolution bug:** `global.d.ts` had a stale blanket `declare module "leaflet";` / `declare module "leaflet-draw";` left over from before `@types/leaflet`/`@types/leaflet-draw` were installed as devDependencies. A bare ambient module declaration silently blanks out real `.d.ts` types for that module, so even with the real type packages correctly installed and listed in `package.json`, every Leaflet-typed symbol was resolving as an error instead of its real shape. Removed the stale declarations now that real types are in place. (If this resurfaces locally after pulling, it's almost always `npm install` not having run yet, or VS Code's TS server holding a stale cache — restart it via "TypeScript: Restart TS Server".) <br><br>**`app/marketplace/` (list, `[id]`, `create`) converted** — no service file existed for marketplace endpoints despite this item's earlier notes claiming one did (that was inaccurate; `services/` had 14 files, not 16, and marketplace wasn't among them). Added `marketplaceService.ts` (12 functions: listings CRUD, offers, messages, escrow pay/dispute, `/user/lands` holdings, `/me`) following the same typed-but-permissive pattern as `landService.ts` (`[key: string]: unknown` on nested land/seller/buyer shapes rather than a guessed full backend schema). All three pages now call the service instead of raw `api.*`. No functional changes; `tsc --noEmit` clean.

**`app/referrals/page.tsx` converted** — already called `referralsService.ts`, but that service typed `referrals`/`rewards` as bare `unknown[]`, pushing all the real shape-knowledge into the page as untyped access. Added `Referral`/`Reward`/`RewardType`/widened `ReferralsDashboard` interfaces to the service instead, matching the fields the page actually reads (`referred_user`, `reward_type`, `amount_kobo`, `discount_percentage`, `units`, `claimed`/`claimed_at`). No functional changes; `tsc --noEmit` clean.

**`app/notifications/page.tsx` converted** — kept the page's own inline `apiFetchNotifications()` rather than routing through `notificationService.fetchNotifications()`: the service memoizes/caches and assumes the paginator always nests under `.data`, while this page needs a fresh, defensively-shaped fetch on every mark-read. Typed it against the existing `AppNotification` interface (already had `data.message`/`read_at`/`created_at`), with local casts for the `units`/`amount_kobo` fields the transaction-detail line reads off `data`. No functional changes; `tsc --noEmit` clean.

**Not yet converted:** **2 `.jsx` files remain** (3 counting `_disabled-sections-reference.jsx`, excluded from lint/typecheck, see #5): `support/page.jsx`, `support/LiveChatView.jsx`. Everything else — `app/admin/`, `app/marketplace/`, `app/referrals/`, `app/notifications/`, `app/dashboard/`, `app/wallet/`, `app/portfolio/` (incl. `certificate/[certNumber]/`), `app/blog/` (list + `[slug]`), `app/forgot-password/`, `app/set-new-password/`, `app/privacy/`, `app/terms/`, `app/settings/kyc/FileDropZone.tsx`, and `app/lands/` (incl. `[id]/`) — is `.tsx`/`.ts`. <br><br>**`dashboard/page.tsx`** converted — typed against `UserStats`/`Transaction` from `userService.ts` and `AuthUser` from `AuthContext.tsx`; added a local `DashboardTransaction` interface for the extra display fields (`amount`, `land`, `units`, `date`, `created_at`) the page reads off transactions. `useAuth()` destructured with the `?? {}` guard pattern (matching `AdminGuard.tsx`/`ProfileSettings.tsx`) since it can return `null`. `tsc --noEmit` clean; no functional changes. <br><br>**`wallet/page.tsx`** converted — added a local `WalletTransaction` interface (extends `Transaction` with `reference`/`amount`/`gateway`/`date`/`created_at`), a `Gateway` interface for the `GATEWAYS` array, and `LoadingKind`/`LoadErrorKind` union types for the `loading`/`loadError` state. Catch blocks now cast `err` through `AxiosError<...>` (matching `handleApiError.ts`'s own signature) instead of relying on implicit `any`. `tsc --noEmit` clean; no functional changes. <br><br>**`portfolio/page.tsx`** converted — added local `PortfolioLand`, `PortfolioSummaryDisplay`, `PortfolioTransaction`, and `ModalState` interfaces. **Two real type-vs-shared-service mismatches surfaced and were handled the same way as the admin batch's `unknown`-return services (#17):** `landService.ts`'s `getUserUnitsForLand()` is typed as `{ units }` but this page has always read `payload.data.available_units`/`payload.data.user_units` off it — asserted through a local `UserUnitsPayload` interface at this one call site rather than widening the shared type (other consumers may genuinely get the `{ units }` shape). Same treatment for `getPurchasePreview()`'s declared `{ [key: string]: unknown }` — added a local `PurchasePreviewData` interface with the fields this page actually renders (`discount_label`, `original_cost_naira`, `total_discount_kobo`, etc.), asserted at the fetch site. Several `preview.*_kobo`/`*_naira` reads were optional in that interface (matching the shared service's honesty about the field being conditional) so comparisons/toLocaleString calls picked up explicit `?? 0` fallbacks to satisfy strict null checks — no behavior change, since `undefined > 0` and `0 > 0` both evaluate falsy the same way the old unguarded access did. `tsc --noEmit` clean; no functional changes. <br><br>**`portfolio/certificate/[certNumber]/page.tsx`** converted — added a local `Certificate` interface and typed `DetailRow`/`Seal` props; download catch block casts through `AxiosError` for status-code branching. `tsc --noEmit` clean; no functional changes. <br><br>**`forgot-password/page.tsx`** and **`set-new-password/page.tsx`** converted — added local `ApiErrorData`/`PasswordForm` interfaces and the same `AxiosError<ApiErrorData>` catch-block pattern used in `wallet/page.tsx`. `PasswordForm` carries an index signature so it satisfies `authService.ts`'s permissive `resetPassword(email, form: Record<string, unknown>)` param. `tsc --noEmit` clean; no functional changes. <br><br>**`privacy/page.tsx`** and **`terms/page.tsx`** converted (server components, no client hooks) — `metadata` typed as `Metadata` (matching the `lands/page.tsx` convention), plus local `PrivacySection`/`TermsSection`/`SummaryRow` interfaces (`content: ReactNode`) for the section-array data. `tsc --noEmit` clean; no functional changes. <br><br>**`blog/page.tsx`** and **`blog/[slug]/page.tsx`** converted — added shared public-facing `BlogPost`/`BlogCategory`/`BlogTag`/`BlogListMeta` interfaces to `blogService.ts` (these two pages fetch directly against `NEXT_PUBLIC_API_URL` rather than through that service's axios client, so the types live there as the shape both pages agree on). `tsc --noEmit` clean; no functional changes. <br><br>**`settings/kyc/FileDropZone.tsx`** converted — props typed against `KycSteps.tsx`'s existing `handleFileChange`/`handleFileError` call signatures, plus a local `SizeInfo` interface and typed refs/drag-and-drop event handlers. `tsc --noEmit` clean; no functional changes. <br><br>Note: `lands/[id]/page.jsx` and `dashboard/page.jsx` were listed here as outstanding in an earlier pass of this doc but were already/are now `.tsx` — that was stale bookkeeping, not a re-conversion in `lands/[id]`'s case. <br><br>**`support/page.tsx`** and **`support/LiveChatView.tsx`** converted — the last two `.jsx` files in `app/`. Added local `TicketMessage`/`TicketDetailData`/`FaqItem`/`FaqGroupsMap`/`NewTicketFormState`/`GuestFormState`/`AiUiMessage` interfaces to the page, and `LiveChatMessage`/`LiveChatTicket`/`Phase`/`WsStatus` to the live-chat view. `useAuth()` destructured with the `?? {}` guard in all five sub-components that call it (`SupportPage`, `TicketList`, `TicketDetail`, `FaqView`, `AiChatView`, `LiveChatView`) — none had the guard before, matching the same gap already fixed in `AdminGuard.tsx`/`SupportWidget.tsx`/`ProfileSettings.tsx`. **Real bug caught and fixed:** `LiveChatView.tsx`'s `broadcastAuthorizer` had the identical `callback(false/true, ...)` vs. real `(Error | null, authData | null)` mismatch already documented for `admin/live-chat/page.jsx` in this same item — worked by accident on truthy/falsy coercion but silently discarded the real error object on auth failure; fixed to match pusher-js's actual `ChannelAuthorizationCallback` contract. `fetchTickets()`'s declared `Promise<SupportTicket[]>` return doesn't match what `TicketList` actually reads off it (a paginator's `.data`) — asserted through a local `TicketListPage` interface at that one call site rather than widening the shared service type, same treatment as the other unknown-return services under #17. `tsc --noEmit` clean repo-wide. No other functional changes.<br><br>**`app/` now has zero `.jsx` files** (only `_disabled-sections-reference.jsx`, deliberately excluded from lint/typecheck per #5/#23). TypeScript adoption for `app/` is complete. <br><br>**`utils/api.ts`** converted — the shared axios instance touched by all 23 files that import it. Added a `RetryableRequestConfig` interface (extends `InternalAxiosRequestConfig` with `retryable?`/`_retry?`/`_retryCount?`) for the opt-in retry flag documented in the retry-logic comment (#8) but not yet used by any call site. Typed the response interceptor's error as `AxiosError`, the request interceptor's config as `InternalAxiosRequestConfig`, and `refreshQueue` as `{ resolve: () => void; reject: (error: unknown) => void }[]`. **Minor behavior note, not a bug:** the queued-caller branch of `refreshAccessToken()` now explicitly resolves with `null` instead of `undefined` when it was piggybacking on an in-flight refresh — `AuthContext.tsx`'s only consumer of the return value already treats a falsy `expires_at` the same way, so this is a type-only clarification, not a functional change. `tsc --noEmit` clean repo-wide; lint clean.
| 3 | ~~Implement proactive token refresh + fix role-reset bug~~ ✅ | Was reactive-only (waited for 401). Now `AuthContext.tsx` schedules a refresh ~5 min before expiry via `scheduleProactiveRefresh`, chained through `api.js`'s shared `refreshAccessToken()`. Also fixed the bug where `setToken(newToken)` on refresh omitted the role arg, defaulting it to `"user"` and silently demoting admins on every refresh — now passes the current role through. *(Superseded by 3a below.)* |
| 3a | ~~Move JWT out of readable cookies~~ ✅ (frontend done; needs matching backend work) | Rewrote the auth model end to end: `utils/tokenStore.ts` no longer reads/writes `auth_token`/`user_role` at all — those are now expected to be httpOnly cookies set by the API via `Set-Cookie` on `/login`, `/refresh`, cleared on `/logout`. The client only reads a non-sensitive `is_authed` flag cookie. `utils/api.js` dropped manual `Authorization: Bearer` logic for `withCredentials: true`; `refreshAccessToken()` reads `expires_at` from the response body. `AuthContext.tsx` updated to match. **Requires a backend contract change** — documented in `tokenStore.ts`: httpOnly Set-Cookie on login/refresh/logout, `expires_at` in `/login`, `/refresh`, `/me` bodies, CORS with `Access-Control-Allow-Credentials` + specific origin. <br><br>Also fixed while in here: dead/duplicate cookie-writing code in `app/login/page.jsx`; live-chat Pusher/Echo auth in `app/admin/live-chat/page.jsx` and `app/support/LiveChatView.jsx` (was reading a nonexistent `token` cookie) now uses a `withCredentials` axios call; `LandList.jsx`'s inline guest check now uses `isAuthed()`. |
| 4 | ~~Introduce React Query (TanStack Query)~~ ✅ (setup + 1 reference conversion) | Added `@tanstack/react-query` + devtools, `app/providers/QueryProvider.tsx` wired into root layout, `utils/queryKeys.ts` as the shared key convention. `staleTime: 30_000`, `refetchOnWindowFocus: false`, `retry: 1`. Devtools only render when `NODE_ENV === "development"` (confirmed — never ships in prod builds). Converted `app/dashboard/page.jsx`'s `useDashboardData` hook to `useQuery` as the reference pattern. Wallet, marketplace, portfolio, admin pages still fetch manually — follow the same pattern there. |
| 5 | ~~Split monolithic components~~ ✅ (both `app/page.jsx` and `SupportWidget.jsx` done, as `.tsx`) | **`app/page.jsx`** decomposed into `app/components/home/` (`HeroSection`, `HowItWorksSection`, `ReturnsSection`, `WhyUsSection`, `FeaturedPropertiesSection`, `LiquiditySection`, `ComplianceSection`, `FaqPageSection`, shared `SectionPrimitives`). `page.tsx` is now a thin orchestrator. The four sections already commented out before this change were preserved verbatim in `app/components/home/_disabled-sections-reference.jsx`. <br><br>**`SupportWidget.jsx`** (597 lines) decomposed into `app/components/support/`: `constants.ts` (shared `FAQ_PREVIEWS`/`TICKET_CATEGORIES`/`GUEST_CATEGORIES`/input classes), `ChatTab`, `FaqTab`, `TicketTab`, `GuestSupportBubble`. `SupportWidget.tsx` is now a 117-line shell. <br><br>Everything from this pass is written as `.tsx` with real prop interfaces (reusing `AuthUser` from `AuthContext`, `ChatMessage`/`FaqGroups`/`TicketPriority` from `supportService`). <br><br>**Bugs found and fixed along the way:** malformed `hover:bg-white/[0.01]0` class typo duplicated in `page.jsx` (already fixed once in `Button.jsx`, #9) → `hover:bg-white/[0.02]`; `GuestSupportBubble` called `createGuestTicket(...)` without ever importing it — a `ReferenceError` on every guest ticket submission, now fixed; dead `statusCfg()` helper in the old `SupportWidget.jsx` (defined, never called — live duplicates of the same helper exist in `app/support/page.jsx` and `app/dashboard/page.jsx`, worth consolidating into one shared util later) was dropped rather than carried forward; `useAuth()` can return `null` per its own type signature but `SupportWidget` destructured it unguarded — fixed with `useAuth() ?? {}`, matching the pattern already established in `AdminGuard.tsx` (and now also applied in `ProfileSettings.tsx`, see #2). <br><br>**Separate discovery, not caused by this split — now resolved:** `SupportWidget` was not imported or rendered anywhere in the app. Mounted it in `app/layout.tsx` inside `AuthProvider` (it calls `useAuth()` internally), alongside `WhatsAppButton`. Found a positioning conflict while wiring it in: `WhatsAppButton` already occupies `fixed bottom-6 right-6` (mobile: `bottom-24 right-4`) — `SupportWidget`/`GuestSupportBubble` used the exact same fixed position, so mounting as-is would have stacked them directly on top of each other. Moved the support bubble to `bottom-44 sm:bottom-24 right-4 sm:right-6` so it sits stacked above the WhatsApp button on both breakpoints instead of overlapping it. |
| 6 | ~~Add React Hook Form + Zod~~ ✅ (setup + 1 reference conversion) | Added `react-hook-form`, `zod`, `@hookform/resolvers`. Converted `app/settings/BankDetails.tsx` as the reference: schema-validated bank code/account number, `Controller` for custom select/input styling, preserving the auto-verify-on-10-digits effect and post-verification lock state. Registration, KYC, and PIN flows still use manual validation. |

---

## 🟠 High Priority

| # | Task | Impact |
|---|------|--------|
| 7 | ~~Deduplicate route constants~~ ✅ | Consolidated into `utils/routes.ts` (`PUBLIC_ROUTES` + `isPublicRoute()`), used by `proxy.js`, `utils/api.js`, `context/AuthContext.tsx`. Fixed real drift: stray `/reset-password` (not a real route), missing `/waitlist`, `/blog`, `/verify`. Fixed inconsistency where `/lands/[id]` was gated in `proxy.js` but public in `api.js` — now consistently gated via `EXACT_MATCH_ONLY_ROUTES`. |
| 8 | ~~Add request retry logic~~ ✅ for transient 5xx errors | `utils/api.js`'s response interceptor retries network errors and 5xx responses, up to 2 retries with exponential backoff + jitter. GET-only by default (auto-retrying a POST purchase/sale/withdrawal risks a double submission); a mutation can opt in via `{ retryable: true }` if genuinely idempotent. |
| 9 | ~~Add `clsx` + `tailwind-merge`~~ ✅ (setup + reference conversions) | Added both plus `utils/cn.ts`. Converted `app/components/Button.jsx`. Found and fixed a real bug: the `ghost` variant's `hover:bg-white/[0.01]0` was a malformed Tailwind arbitrary-value class (stray trailing `0`) that silently generated nothing — fixed to `hover:bg-white/[0.02]`. The identical typo turned up repeatedly elsewhere (`page.jsx` during #5; `LivenessCheck.tsx` and `KycPanel.tsx` during the settings/KYC TS pass in #2) and was fixed each time it was found. A dedicated upstream commit ("normalize Tailwind opacity utility classes") has since swept the whole repo to collapse arbitrary-bracket opacity values (`bg-white/[0.02]`) to plain shorthand (`bg-white/2`) — that's now the preferred form going forward. |
| 10 | ~~Add date formatting utility~~ ✅ (setup + 1 reference conversion) | Added `date-fns` and `utils/date.ts` (`formatDate`, `formatDateTime`, `formatTime`, `formatTimeAgo` — null-safe, returns `"—"` instead of throwing). Converted `NotificationBell.tsx` (now `.tsx`, converted upstream) to `formatTimeAgo`. ~15 other files still do manual `toLocaleDateString`/`toLocaleString` formatting — worth sweeping over, left as-is for now (numerous but low-risk). |
| 11 | ~~Fix `logout()` to use `router.push()`~~ ✅ | `context/AuthContext.tsx`'s `logout()` now awaits the `/logout` call (was fire-and-forget, a real race that could leave httpOnly cookies uncleared) and uses `router.push("/login")` instead of `window.location.href`. Also calls `queryClient.clear()` before navigating, since React Query (#4) now caches dashboard/wallet data and a stale cache could flash the previous user's data on a shared device. |
| 12 | ~~Add client-side image preview + crop before KYC upload~~ ✅ (reviewed and converted) | Added `app/settings/kyc/CropModal.jsx`, now converted to `CropModal.tsx`. No correctness issues found in the crop logic itself. One type-resolution snag: `react-easy-crop`'s `Area`/`Point` types live at `react-easy-crop/types`, but the package's `exports` map only exposes its root entry under `"bundler"` module resolution, so that subpath isn't reachable — worked around by deriving the same shapes structurally off `Cropper`'s own public prop types (`ComponentProps<typeof Cropper>`) instead of importing them directly. |
| 13 | ~~Add error boundary + fallback UI~~ ✅ (fixed placement bug) | `error.jsx`/`global-error.tsx` originally landed at the **repo root** instead of inside `app/` — in the Next.js App Router, error boundaries only function when placed inside `app/`, so they were silently inert despite being marked done. This was caught in review and has since been fixed upstream: both files now live correctly under `app/` (`app/error.jsx`, `app/global-error.tsx`). No nested `admin`/`dashboard`/`settings` layouts exist, so the root boundary covers all routes. |

---

## 🟡 Medium Priority

| # | Task | Impact |
|---|------|--------|
| 14 | ~~Add loading skeletons~~ ✅ (dashboard + portfolio done) | `app/dashboard/page.jsx` has a `SkeletonCard`. `app/portfolio/page.jsx` has a full set — `SummaryCardSkeleton`, `LandCardSkeleton`, `TransactionRowSkeleton` — all `animate-pulse`-based, matching per-element rather than whole-card so nothing jumps when real data arrives. Confirmed via audit — no `Skeleton` component library needed, plain Tailwind was sufficient. Other pages using `animate-pulse`/spinners already: `admin` (root, compliance, lands create/edit), `marketplace`, `blog`, `notifications`, `support` (+ `LiveChatView`) — worth a pass to standardize naming/structure but functionally covered. |
| 15 | ~~Add `zustand` or `jotai`~~ ✅ (scoped, narrow) | Added `zustand`. `stores/useUIStore.ts` currently holds just one piece of shared state: a `confirmState` object backing a shared `<ConfirmDialog />` (new — `app/components/ConfirmDialog.tsx`) and a `useConfirm()` hook that mirrors `window.confirm()`'s ergonomics but returns a promise and renders the app's own styled dialog. Scoped intentionally — deliberately *not* a general-purpose store; `AuthContext` still owns auth, `react-hot-toast` still owns toasts. Motivation: native `window.confirm()` was duplicated with no shared styling across 9 admin pages — worth sweeping those call sites over to `useConfirm()` next. |
| 16 | ~~Add `next/image` optimization~~ ✅ (all genuine candidates converted) | Converted the 8 real candidates: `app/marketplace/page.jsx` (listing thumbnails, `fill`), `marketplace/[id]/page.jsx` (detail hero), `marketplace/create/page.jsx` (48×48 fixed-size thumbnail), `app/blog/page.jsx` (listing cover), `blog/[slug]/page.jsx` (detail hero, `priority`), `app/lands/[id]/page.jsx` (`SlideTile` photo grid, `priority` on first tile), `app/admin/lands/[id]/edit/page.jsx` (existing/remote images only — left the adjacent `newImagePreviews` block alone, see below), and `app/components/Card.tsx` (dead/unused component, fixed for correctness anyway). All use `fill` + `sizes` (or fixed `width`/`height` for the one small thumbnail) matching existing `relative`-positioned wrappers. <br><br>**Config fix required:** `next.config.mjs`'s `remotePatterns` only allowlisted `pathname: '/lands/**'` on the R2 host — blog cover images live under a different path prefix on the same bucket and would have 400'd once switched to `next/image`'s optimizer. Broadened to `pathname: '/**'` on that single already-trusted hostname (not a new origin, just a wider path on the same one). <br><br>**Behavior note:** `next/image` doesn't support imperative `onError` src-swapping the way raw `<img>` did — converted the two files that had onError-based fallback-to-placeholder logic (`marketplace/page.jsx`, `lands/[id]/page.jsx`) to a `useState`-backed controlled fallback instead (swap to a placeholder icon or `/no-image.jpeg` via state, same end result). <br><br>**Deliberately left as `<img>`, not gaps:** local blob-URL upload previews before submission (`admin/lands/create/page.jsx`'s `imagePreviews`, the `newImagePreviews` half of `admin/lands/[id]/edit/page.jsx`, `admin/blog/page.jsx`'s `coverPreview`, and the KYC upload/crop/liveness components) — these are ephemeral local object URLs `next/image`'s remote optimizer can't and shouldn't process. Also `admin/support/page.jsx` and `support/page.jsx`'s chat attachment previews, which fetch an authenticated blob at runtime the same way `AuthImage.tsx` does (needs auth headers the image optimizer can't attach) — same accepted exception noted for `AuthImage.tsx` itself. `WhatsAppButton.tsx`'s 28×28 external CDN icon (`cdn.jsdelivr.net`) is low-value/decorative and would need its own new allowlisted origin — left as-is, worth a follow-up only if bundling that icon locally isn't simpler. <br><br>**Separate finding, not part of this item:** `app/components/LandImage.tsx` is unused dead code — nothing imports the component (a same-named-but-different `getLandImage()` *util* from `utils/images.js` is what's actually used elsewhere). Same pattern as the `ThemeToggle.jsx` removal in #18 — flagged here, not deleted without confirming first. |
| 17 | ~~Add service layer for all API calls~~ ✅ | Only 3 service files existed for 30+ backend endpoints; now 16 service files (`userService`, `walletService`, `authService`, `bankService`, `pinService`, `kycService`, `landService`, `portfolioService`, `marketplaceService`, `notificationService`, `referralsService`, `mediaService`, `supportService`, `blogService`, `complianceService`, `adminService`) cover every domain, with typed request/response shapes throughout. ~45 pages/components converted across auth, settings, lands, portfolio, marketplace, notifications, referrals, support, and all of `app/admin/` (blog, compliance, kyc, lands, live-chat, referrals, support, users, waitlist, withdrawals, dashboard). <br><br>**Notable extractions along the way:** `mediaService.ts`'s `fetchAuthedBlob`/`toRelativePath` deduplicated two near-identical implementations that had drifted between `AuthImage.tsx` and `useAuthImage.js`. `landService.ts`'s `purchaseLand` gained a `useRewards` param to unify what had been two separate inline implementations in `lands/[id]/page.jsx` and `portfolio/page.jsx`. `adminService.ts`'s `getAdminDashboardStats()` collapsed ~100 lines of inline `Promise.allSettled` parsing (18 parallel count-endpoint fetches) in `admin/page.jsx` into a single typed call. <br><br>**Deliberately left on raw `api.*`, not gaps:** `context/AuthContext.tsx` (owns login/logout/me as the auth source of truth — intentionally not routed through a service); `app/notifications/page.jsx`'s fetch (a documented paginator-shape workaround — its mark-read mutations do go through `notificationService`). <br><br>Repo-wide `grep` confirms no other raw `api.*` calls remain under `app/`. `tsc --noEmit` clean at every commit in this batch. |
| ~~18~~ | ~~Add dark mode persistence~~ ❌ removed | `ThemeToggle.jsx` was dead code — it imported a `context/ThemeContext` module that didn't exist anywhere in the repo (a `ReferenceError` waiting to happen) and nothing imported `ThemeToggle` itself. Confirmed the app is single-theme by design, so there's no dark mode to persist. Deleted rather than converted. |
| 19 | **Add PWA support** (`next-pwa`) | Land investment users are mobile-first in Nigeria. Offline access to portfolio data is valuable. Confirmed `next-pwa` is not installed yet. |
| 20 | ~~Add form auto-save for KYC~~ ✅ | Added `utils/kycDraft.ts` (`saveKycDraft`/`loadKycDraft`/`clearKycDraft`/`isDraftMeaningful`), debounced (800ms) auto-save to `localStorage` while `KycPanel.tsx`'s form is open. **Scoped deliberately to text/select fields only** — `id_front`/`id_back`/`selfie` are `File` objects and can't survive `JSON.stringify`, so a restored draft clamps its step down to the end of the Identity step (3); Documents and Liveness always have to be redone after a restore, which is an accepted trade-off rather than a gap. Restores + toasts on mount only when a meaningful draft exists *and* the fetched KYC status is still editable (`not_submitted`/`rejected`/`resubmit`) — never overwrites an approved/pending submission. Draft is cleared on successful submit. |

---

## 🟢 Polish & DevEx

| # | Task | Impact |
|---|------|--------|
| 21 | ~~Standardize component naming~~ ✅ (partial) | `card.jsx` → `Card.tsx` done as part of the TS migration. Sweep remaining components for PascalCase/camelCase consistency separately. |
| 22 | **Add `CONTRIBUTING.md`** | Linting (`eslint`), testing, branch conventions, and component folder structure rules. Still missing. |
| 23 | ~~Add CI/CD pipeline~~ ✅ (GitHub Actions) | `.github/workflows/ci.yml` runs on push/PR to `main` with 3 parallel jobs: `lint`, `typecheck` (`tsc --noEmit`), and `build` (`next build`, uses placeholder `NEXT_PUBLIC_*` env vars since the build doesn't call the API but `next/image` and a few modules read them at build time) — all three are now hard gates. Added `.env.example`. `eslint.config.mjs` excludes `app/components/home/_disabled-sections-reference.jsx` from lint. <br><br>**Lint cleanup landed:** `lint` started non-blocking (`continue-on-error: true`) due to ~63 pre-existing `react/no-unescaped-entities` errors plus several real `react-hooks/set-state-in-effect`, `react-hooks/purity`, and `react-hooks/refs` errors surfaced once the React Compiler ESLint rules were in place. All fixed — apostrophes/quotes escaped throughout; synchronous `setState` calls in effect bodies replaced with render-time derivation, the "adjust state during render" guarded pattern, tagged-result staleness checks, or microtask/async-IIFE deferral where real side effects (media tracks, rAF, `Date.now()`) were also involved; three "latest callback" ref assignments moved from the render body into effects. `npm run lint` is now 0 errors repo-wide (only pre-existing `next/no-img-element`/`jsx-a11y/alt-text`/`exhaustive-deps` warnings remain) — `continue-on-error` removed, lint is a real gate now. <br><br>Also bumped `node-version: 20` → `24` in all three jobs — GitHub Actions was silently force-upgrading `actions/checkout@v4`/`actions/setup-node@v4` off Node 20 with a deprecation warning on every run. |
| 24 | **Add Storybook** for shared components | `Button`, `Input`, `Card`, `OTPInput`, `PinInput`, and now `ConfirmDialog` should have isolated documentation and visual regression tests. |
| 25 | **Add `next-bundle-analyzer`** | `page.jsx` was 39KB un-gzipped before the #5 split into section components — re-measure post-split, then identify and split heavy dependencies (Tiptap, Leaflet) into dynamic imports. |
| 26 | ~~Add `robots.txt` + meta tags~~ ✅ (fixed, not added — it already existed) | `public/robots.txt` was already present and reasonably configured, contrary to this item's original wording. Found and fixed a real inconsistency: it disallowed `/reset-password`, a route that doesn't exist (item #7 already established the real route is `/reset-verify` — this was the same stray-route drift, just in `robots.txt` instead of `routes.ts`). Fixed to `Disallow: /reset-verify`, and added the missing `/verify`, `/verify-email`, `/notifications` (all authenticated/no-index routes that weren't listed). `app/sitemap.js` also exists and pulls live land slugs from the API. Still open: verify per-route SEO meta (title, description, OG tags) across all public routes — not checked in this pass. |

---

*Last updated: 2026-08-15 — Item #2 (Add TypeScript) is now fully complete, including `utils/api.ts` (the shared axios instance, 23 consumers). `app/` contains zero `.jsx` files aside from the deliberately-excluded `_disabled-sections-reference.jsx`. `tsc --noEmit` clean repo-wide. Full-repo audit found 8 remaining `.js`/`.jsx` files (see #2 for the list) — `services/`, `stores/`, `context/`, and now `utils/api.ts` are all typed; the rest are small utilities or Next.js framework files conventionally left as `.js`. A possible auth-cookie drift in `proxy.js` was flagged for follow-up, not fixed.*